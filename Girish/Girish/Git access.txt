which git

yum install git

yum install git git-all

vi /etc/cagefs/conf.d/git.cfg

```
[git]
comment=Allow git to work on the server
paths=/usr/local/bin, /usr/bin, /usr/libexec/git-core

```
 cagefsctl --addrpm git

cagefsctl --force-update

git ls-remote git@github.com:mlajeunesse/acetrack.git HEAD


================

